import { type Routes } from '@angular/router';

import { HealthCheckComponent } from '../presentation/health-check.component';

export const HEALTH_CHECK_ROUTES: Routes = [{ path: '', component: HealthCheckComponent }];
